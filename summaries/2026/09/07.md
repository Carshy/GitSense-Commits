# Activity Summary for 9/7/2026

## 2:23:53 PM
The provided log details the foundational setup and progressive schema definition for a Nuraya Fragrance e-commerce project, primarily focused on backend and database architecture using Drizzle ORM, Next.js, and TypeScript.

**Key Information & File-Specific Updates:**

*   **Project Initialization & Tooling (9/3/2026):**
    *   **`setup-project.sh` (8:02:02 PM):** This crucial script initiates the project structure, creating a modular architecture with directories for components, database schemas/migrations, libraries (auth, cache, logger, security, email, storage, validation, errors), configuration, hooks, types, styles, and tests. It explicitly sets up `src/modules` for various business domains like `auth`, `users`, `catalog`, `orders`, `payments`, `shipping`, `promotions`, and `reviews`, each with `domain`, `application`, `infrastructure`, and `presentation` layers. It also touches essential files like `src/db/index.ts` and `src/config/env.ts`.
    *   **`tsconfig.json` (4:48:05 PM, 8:41:06 PM):** Initially configures TypeScript for ES2017, Next.js, and path aliases. Later, it adds stricter checks like `noUnusedLocals`, `noUnusedParameters`, and `forceConsistentCasingInFileNames`.
    *   **`package.json` (8:46:48 PM, 9:03:55 PM):** Defines standard Next.js scripts, adds `zod` for validation, and later integrates Drizzle ORM by adding `drizzle-orm`, `postgres`, `bcryptjs`, `pino` dependencies, and Drizzle-specific CLI scripts (`db:generate`, `db:migrate`, `db:push`, `db:studio`).
    *   **`.prettierrc` (8:44:23 PM) and `.prettierignore` (8:45:06 PM):** Establish code formatting rules (e.g., single quotes, trailing commas) and specify files/directories to exclude from formatting.
    *   **`src/config/env.ts` (8:22:43 PM):** Sets up environment variable validation using Zod for `NODE_ENV`, `DATABASE_URL`, `NEXT_PUBLIC_APP_URL`, and `AUTH_SECRET`.
    *   **`drizzle.config.ts` (8:58:31 PM, 9:02:41 PM):** Configures DrizzleKit to point to the schema files, migration output, PostgreSQL dialect, and to use `DATABASE_URL` from environment variables.
    *   **`src/lib/errors/app-error.ts` (8:49:05 PM) and `src/lib/errors/error-codes.ts` (10:37:01 PM):** Define a custom `AppError` class with status codes and a standardized set of error codes.
    *   **`src/lib/logger/index.ts` (8:52:26 PM):** Integrates Pino for logging, with level adjustment based on the environment.
    *   **`src/lib/security/hash.ts` (8:55:16 PM):** Provides utility functions for password hashing and verification using `bcryptjs`.
    *   **`src/db/index.ts` (10:32:50 PM):** Initializes the Drizzle ORM client using `postgres-js` and the `DATABASE_URL` from the environment.

*   **Database Schema Definitions & Relations (9/4/2026 - 9/7/2026):**
    *   **`src/db/schema/helpers.ts` (9/4/2026, 9:54:05 PM):** Introduces a `timestamps` helper to consistently add `createdAt` and `updatedAt` fields to tables. Many tables also adopt a `deletedAt` field for soft deletes.
    *   **Core User/Auth Schemas (`users`, `roles`, `permissions`, `user-roles`, `role-permissions`, `sessions`):** These schemas were defined on **9/4/2026** and then extensively refined on **9/5/2026** to include database indices (`email`, `phone`, `status` for users; `name` for roles; `code` for permissions; `tokenHash`, `userId` for sessions; `userId`, `roleId` for user-roles; `roleId`, `permissionId` for role-permissions) and Drizzle ORM relations in `src/db/schema/relations.ts`.
        *   `users.schema.ts` also received an import `boolean` on 9/7/2026.
    *   **Customer & Guest Schemas (`customer-profiles`, `addresses`, `guest-checkout-sessions`):** Introduced on **9/5/2026 (3:58 PM - 4:03 PM)**.
        *   `customer-profiles` links to `users`, stores personal info, and has marketing opt-in.
        *   `addresses` links to `customer-profiles`, supports shipping/billing types, and includes full address details.
        *   `guest-checkout-sessions` tracks guest checkout data, including potential conversion to a registered user.
        *   `src/db/schema/customer-relations.ts` (9/5/2026, 4:04:57 PM) defines their relationships.
    *   **Audit Logging Schema (`audit-logs`):** Defined on **9/5/2026 (8:55:26 PM)**, capturing actions, entity types/IDs, old/new values, IP address, user agent, and user associations. `src/db/schema/audit-relations.ts` (9/5/2026, 8:58:50 PM) defines its relation to `users`.
    *   **Catalog & Product Management Schemas (`brands`, `categories`, `products`, `product-variants`, `inventory`, `pricing`, `product-images`, `fragrance-notes`, `product-notes`):** These were added progressively on **9/5/2026 (9:41 PM - 10:20 PM)**.
        *   `brands` and `categories` manage product classifications.
        *   `products` links to brands and categories, includes SEO fields and a `product_status` enum.
        *   `product-variants` define specific product options (SKU, size, fragrance concentration), linking to `products`.
        *   `inventory` tracks quantity and reserved quantity for product variants.
        *   `pricing` stores currency and amount for variants, with active dates.
        *   `product-images` store image URLs for products.
        *   `fragrance-notes` defines distinct fragrance notes.
        *   `product-notes` is a join table linking products to fragrance notes by type (TOP, MIDDLE, BASE).
        *   `src/db/schema/catalog-relations.ts` (9/5/2026, 10:22:03 PM onwards, with updates on 9/6/2026) was built out to define the complex interconnections within the catalog.
    *   **`products.schema.ts` specific changes (9/6/2026, 10:00 AM - 9/7/2026, 2:16 PM):** This file saw iterative changes regarding an `isVisible` field. It was initially added to the `productStatusEnum` (incorrectly), then moved to a boolean column, then removed entirely, and finally re-introduced as a `boolean("is_visible").default(false).notNull()` column on 9/7/2026.

*   **Frontend Entry Point (9/7/2026):**
    *   **`src/app/page.tsx` (8:52:51 AM, 8:54:26 AM):** The main Next.js page component was created, displaying "Welcome to Nuraya Fragrance". A minor cleanup removed an unused `Image` import shortly after creation.

**Patterns and Recurring Elements:**

1.  **Drizzle ORM & PostgreSQL Focus:** The project heavily leverages Drizzle ORM for schema definition, relationships, and database interactions, with a PostgreSQL backend.
2.  **Modular Database Schemas:** Schemas are organized into individual files per entity, promoting maintainability.
3.  **Comprehensive Indexing:** Many tables include explicit indices on foreign keys, unique fields, and commonly filtered columns (e.g., `status`, `createdAt`) for query optimization.
4.  **UUIDs for Primary Keys:** Universally applied for primary keys, defaulting to random generation.
5.  **Timestamp and Soft Delete Fields:** The `timestamps` helper is consistently used, and `deletedAt` is present in most core tables, indicating a soft-delete strategy.
6.  **Enum-Driven Fields:** Custom PostgreSQL enums are used extensively to enforce valid states/types for various fields (e.g., `user_status`, `address_type`, `product_status`).
7.  **`onDelete` Foreign Key Actions:** `onDelete: "cascade"` is common for strong relationships, while `onDelete: "set null"` is used for optional associations (e.g., `guestCheckoutSessions.convertedUserId`, `auditLogs.userId`).
8.  **Layered Architecture:** The `setup-project.sh` script outlines a domain-driven design with explicit `domain`, `application`, `infrastructure`, and `presentation` layers for each module.
9.  **Iterative Development:** The log shows a clear progression from basic project setup and core user management, to customer and audit functionalities, and finally to a detailed e-commerce catalog structure. Revisions like those in `products.schema.ts` regarding `isVisible` highlight an iterative design process.