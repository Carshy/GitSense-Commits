# Activity Summary for 9/13/2026

## 12:35:11 PM
The log details schema evolution for a "nuraya-fragrance" project, primarily focusing on Drizzle ORM definitions in PostgreSQL.

**File-Specific Updates:**

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts**
    *   **9/13/2026, 11:11:23 AM:** Initial schema creation for `productVariants` table, defining columns such as `id` (UUID primary key), `productId` (foreign key to `products` with `onDelete: "cascade"`), `sku` (unique), `barcode`, `sizeValue`, `sizeUnit` (default "ML"), `concentration` (using `fragrance_concentration` enum), `isDefault`, `status` (using `variant_status` enum, default "ACTIVE"), and common `timestamps` fields including `deletedAt`. Indexes were created for `productId` and `sku`.
    *   **9/13/2026, 11:11:52 AM:** Added `import { sql } from "drizzle-orm";`.
    *   **9/13/2026, 11:15:26 AM:** Introduced a `check` constraint named `"product_variants_size_value_positive"` to ensure `sizeValue` is greater than 0.
    *   **9/13/2026, 11:17:42 AM:** The `check` constraint was accidentally duplicated within the table's index definition array.
    *   **9/13/2026, 11:18:43 AM:** The redundant `check` constraint was removed from the index definition, leaving it correctly at the table level.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts**
    *   **9/13/2026, 11:19:29 AM:** Initial schema for `inventory` table, including `id` (UUID primary key), `variantId` (unique foreign key to `productVariants` with `onDelete: "cascade"`), `quantity` (default 0), `reservedQuantity` (default 0), `createdAt`, and `updatedAt`. An index on `variantId` was defined.
    *   **9/13/2026, 11:19:49 AM:** Added `import { sql } from "drizzle-orm";`.
    *   **9/13/2026, 11:20:35 AM:** Added three `check` constraints: `"inventory_quantity_non_negative"` (`quantity >= 0`), `"inventory_reserved_quantity_non_negative"` (`reservedQuantity >= 0`), and `"inventory_reserved_not_exceed_quantity"` (`reservedQuantity <= quantity`).

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts**
    *   **9/13/2026, 11:21:37 AM:** Initial schema for `pricing` table, with `id` (UUID primary key), `variantId` (foreign key to `productVariants` with `onDelete: "cascade"`), `currency` (default "KES"), `amount`, `compareAtAmount` (nullable), `activeFrom` (nullable), `activeUntil` (nullable), and `createdAt`. An index on `variantId` was defined.
    *   **9/13/2026, 11:22:13 AM:** Added two `check` constraints: `"pricing_amount_non_negative"` (`amount >= 0`) and `"pricing_compare_at_amount_non_negative"` (`compareAtAmount IS NULL OR compareAtAmount >= 0`).

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts**
    *   **9/13/2026, 11:23:33 AM:** Initial schema for `inventoryTransactions` table. It defines `id` (UUID primary key), `inventoryId` (foreign key to `inventory` with `onDelete: "restrict"`), `type` (using `inventory_transaction_type` enum), `quantityChange`, `reason` (nullable), `performedBy` (foreign key to `users` with `onDelete: "set null"`), and `createdAt`. Multiple indexes were created for `inventoryId`, `type`, `performedBy`, and `createdAt`.
    *   **9/13/2026, 11:23:46 AM:** Added `import { sql } from "drizzle-orm";`.
    *   **9/13/2026, 11:24:22 AM:** Added a `check` constraint named `"inventory_transactions_quantity_change_non_zero"` to ensure `quantityChange` is not zero.
    *   **9/13/2026, 11:24:54 AM:** No functional code changes.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts**
    *   **9/13/2026, 11:28:35 AM:** This file defines Drizzle ORM `relations` for various tables. It establishes connections between `products`, `brands`, `categories`, `productVariants`, `productImages`, `productNotes`, `fragranceNotes`, `inventory`, `pricing`, `inventoryTransactions`, and `users`, specifying one-to-one and one-to-many relationships.

**Patterns and Recurring Elements:**

*   **Drizzle ORM Usage:** All changes consistently use Drizzle ORM for defining database schemas, including `pgTable`, `uuid`, `varchar`, `integer`, `boolean`, `pgEnum`, `index`, `timestamp`, and `check` functions.
*   **UUID Primary Keys:** All tables utilize `uuid("id").defaultRandom().primaryKey()` for their primary identifiers, ensuring unique, randomly generated IDs.
*   **Timestamp Fields:** `createdAt` and `updatedAt` fields (often defaulting to `defaultNow()`) are common across tables, with `deletedAt` also present in `productVariants` (likely for soft deletes). A `timestamps` helper is explicitly used in `productVariants`.
*   **`import { sql } from "drizzle-orm";` and `check` Constraints:** A clear pattern emerged where `import { sql } from "drizzle-orm";` was added to schema files just before `check` constraints were introduced. These constraints leverage raw SQL to enforce data integrity rules such as non-negative values, non-zero changes, or logical relationships between columns, enhancing data validation at the database level.
*   **Foreign Key Relationships:** Foreign key constraints are consistently defined using `.references()` with `onDelete` actions (e.g., `"cascade"`, `"restrict"`, `"set null"`) to manage referential integrity.
*   **Indexes for Performance:** All tables define explicit indexes on foreign key columns or frequently queried fields to optimize database query performance.
*   **Enum Types:** PostgreSQL `pgEnum` is used to define discrete sets of allowed values for certain columns, such as `fragrance_concentration`, `variant_status`, and `inventory_transaction_type`.

## 1:35:10 PM
The changes primarily focus on defining and refining database schemas using Drizzle ORM for a "nuraya-fragrance" project, spanning several key entities related to products, inventory, pricing, and carts.

**File-Specific Updates:**

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts`**
    *   **Initial Definition (9/13/2026, 11:11:23 AM):** Established the `product_variants` table with fields including `id`, `productId` (referencing `products.schema`), `sku`, `barcode`, `sizeValue`, `sizeUnit`, `concentration` (using `fragrance_concentration` enum), `isDefault`, `status` (using `variant_status` enum), and standard timestamps. It also defined indices on `productId` and `sku`.
    *   **Minor Import (9/13/2026, 11:11:52 AM):** Added an import for `sql` from `drizzle-orm`.
    *   **Schema Refinement (9/13/2026, 11:15:26 AM - 11:18:43 AM):** Introduced a `check` constraint `product_variants_size_value_positive` to ensure `sizeValue` is greater than 0. A temporary duplication of this check constraint was introduced and then resolved within the span of these timestamps.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts`**
    *   **Initial Definition (9/13/2026, 11:19:29 AM):** Created the `inventory` table with `id`, `variantId` (referencing `product-variants.schema`), `quantity`, `reservedQuantity`, `createdAt`, and `updatedAt`, including an index on `variantId`.
    *   **Minor Import (9/13/2026, 11:19:49 AM):** Added an import for `sql` from `drizzle-orm`.
    *   **Schema Refinement (9/13/2026, 11:20:35 AM):** Enhanced data integrity by adding three `check` constraints: `inventory_quantity_non_negative`, `inventory_reserved_quantity_non_negative`, and `inventory_reserved_not_exceed_quantity`, ensuring quantities are non-negative and reserved quantity does not exceed total quantity.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts`**
    *   **Initial Definition (9/13/2026, 11:21:37 AM):** Defined the `pricing` table with `id`, `variantId` (referencing `product-variants.schema`), `currency`, `amount`, `compareAtAmount`, `activeFrom`, `activeUntil`, and `createdAt`, along with an index on `variantId`.
    *   **Schema Refinement (9/13/2026, 11:22:13 AM):** Added two `check` constraints: `pricing_amount_non_negative` and `pricing_compare_at_amount_non_negative`, ensuring pricing values are non-negative or null.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts`**
    *   **Initial Definition (9/13/2026, 11:23:33 AM):** Created the `inventory_transactions` table, including the `inventory_transaction_type` enum. Fields include `id`, `inventoryId` (referencing `inventory.schema`), `type`, `quantityChange`, `reason`, `performedBy` (referencing `users.schema`), and `createdAt`. Multiple indices were defined for various fields.
    *   **Minor Import (9/13/2026, 11:23:46 AM):** Added an import for `sql` from `drizzle-orm`.
    *   **Schema Refinement (9/13/2026, 11:24:22 AM - 11:24:54 AM):** Added a `check` constraint `inventory_transactions_quantity_change_non_zero` to ensure that `quantityChange` is not zero, with no further changes in the subsequent entry.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts`**
    *   **Comprehensive Relation Definition (9/13/2026, 11:28:35 AM):** This file was created to centralize the definition of relations between various Drizzle ORM schemas. It established intricate one-to-many and many-to-one relationships for `products`, `brands`, `categories`, `productVariants`, `productImages`, `productNotes`, `fragranceNotes`, `inventory`, `pricing`, `inventoryTransactions`, and `users`. This consolidates how different parts of the catalog are interconnected.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/carts.schema.ts`**
    *   **Initial Definition (9/13/2026, 1:30:22 PM):** Defined the `carts` table and the `cart_status` enum. The table includes `id`, `userId` (referencing `users.schema`), `guestCheckoutSessionId` (referencing `guest-checkout-sessions.schema`), `status`, `expiresAt`, `createdAt`, and `updatedAt`. It includes indices and a crucial `check` constraint `carts_exactly_one_owner` to enforce that a cart is owned by either a registered user or a guest session, but not both or neither.

**Patterns and Recurring Elements:**

*   **Drizzle ORM for Schema Definition:** All changes consistently utilize Drizzle ORM for defining PostgreSQL tables, columns, enums, indices, and constraints.
*   **UUID Primary Keys:** Most tables use `uuid("id")` as a `defaultRandom().primaryKey()`.
*   **Foreign Key Relationships:** Extensive use of `references()` to link tables, often with `onDelete` actions like `"cascade"`, `"restrict"`, or `"set null"`.
*   **Enum Types for Statuses:** Custom `pgEnum` types are used for fields like `concentration`, `variantStatus`, `inventoryTransactionType`, and `cartStatus` to restrict allowed values.
*   **Standard Timestamps:** `createdAt` and `updatedAt` fields with `defaultNow()` are common across tables, with `product-variants.schema.ts` using a `...timestamps` helper. `deletedAt` is also present for soft deletes.
*   **Importing `sql` and Adding `check` Constraints:** A clear pattern emerged where `import { sql } from "drizzle-orm";` was added to schema files, immediately followed by the introduction of `check` constraints that leverage `sql` literal expressions to enforce complex business rules and data validity (e.g., non-negative values, quantity relationships, exclusive ownership).
*   **Indexing for Performance:** Indices are consistently added on foreign keys and frequently queried columns to optimize database performance.

## 2:35:24 PM
The provided log details significant schema definition and relationship updates for a Drizzle ORM-based PostgreSQL database within a project named "nuraya-fragrance." The changes span across various core e-commerce functionalities, including product catalog, inventory, pricing, shopping carts, and checkout processes.

### File-Specific Updates:

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts`**:
    *   **Initially (9/13/2026, 11:11:23 AM)**: Defined the `productVariants` table with `id`, `productId` (FK to `products`), `sku` (unique), `barcode`, `sizeValue`, `sizeUnit`, `concentration` (using `fragrance_concentration` enum), `isDefault`, `status` (using `variant_status` enum), and soft-delete capabilities (`deletedAt`) in addition to standard timestamps. Indices were set on `productId` and `sku`.
    *   **Later (9/13/2026, 11:15:26 AM - 11:18:43 AM)**: The schema was refined by introducing an `sql` import and adding a `check` constraint to ensure `sizeValue` is always positive (`> 0`). This constraint's placement was adjusted from being a top-level table argument to being defined within the `table` callback alongside indices.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts`**:
    *   **Initially (9/13/2026, 11:19:29 AM)**: Established the `inventory` table, linking to `productVariants` via `variantId` (unique). It included `quantity` and `reservedQuantity`, both defaulting to 0, along with creation and update timestamps. An index was created on `variantId`.
    *   **Later (9/13/2026, 11:20:35 AM)**: After importing `sql`, the schema was enhanced with multiple `check` constraints to enforce data integrity: `quantity` and `reservedQuantity` must be non-negative, and `reservedQuantity` must not exceed `quantity`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts`**:
    *   **Initially (9/13/2026, 11:21:37 AM)**: Created the `pricing` table with a `variantId` (FK to `productVariants`), `currency` (default "KES"), `amount`, and optional `compareAtAmount`, `activeFrom`, and `activeUntil` fields, plus a `createdAt` timestamp. An index was added for `variantId`.
    *   **Later (9/13/2026, 11:22:13 AM)**: `sql` was imported, and `check` constraints were added to ensure `amount` is non-negative and `compareAtAmount` is either `NULL` or non-negative.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts`**:
    *   **Initially (9/13/2026, 11:23:33 AM)**: Defined the `inventoryTransactions` table using an `inventory_transaction_type` enum (including types like "RESTOCK", "SALE", "RESERVATION"). It tracked `inventoryId` (FK to `inventory`), `type`, `quantityChange`, an optional `reason`, and `performedBy` (FK to `users`, nullable on delete). Multiple indices were added for efficient querying.
    *   **Later (9/13/2026, 11:24:22 AM)**: The schema was updated to import `sql` and include a `check` constraint to ensure `quantityChange` is never zero, indicating every transaction must involve a change in quantity.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts` (9/13/2026, 11:28:35 AM)**: This file was introduced to centralize the Drizzle ORM `relations` definitions for the entire product catalog and inventory domain. It established one-to-many and one-to-one relationships between `products`, `brands`, `categories`, `productVariants`, `productImages`, `productNotes`, `fragranceNotes`, `inventory`, `inventoryTransactions`, and `users`, clarifying how these entities are linked.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/carts.schema.ts`**:
    *   **Initially (9/13/2026, 1:30:22 PM)**: Defined the `carts` table, which supports both registered `userId` (FK to `users`) and `guestCheckoutSessionId` (FK to `guestCheckoutSessions`), both nullable. It included `cart_status` enum (e.g., "ACTIVE", "ABANDONED"), `expiresAt`, and standard timestamps. Indices were created for various fields, and a `check` constraint `carts_exactly_one_owner` ensured that a cart is associated with either a user or a guest session, but not both.
    *   **Later (9/13/2026, 1:44:47 PM - 1:45:46 PM)**: `uniqueIndex` was imported, and two unique indices were added: `carts_active_user_unique` and `carts_active_guest_unique`. These prevent a single user or guest from having more than one 'ACTIVE' cart simultaneously.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-items.schema.ts` (9/13/2026, 1:47:17 PM)**: This new schema defined the `cartItems` table, linking to `carts` and `productVariants`. It stored `quantity`, `createdAt`, and `updatedAt`. It also included indices for `cartId` and `productVariantId`, a `uniqueIndex` `cart_items_cart_variant_unique` to ensure a variant appears once per cart, and a `check` constraint `cart_items_quantity_positive` for positive quantities.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-relations.ts` (9/13/2026, 1:49:04 PM)**: This file was added to define the Drizzle ORM relations for `carts` and `cartItems`, specifying their connections to `users`, `guestCheckoutSessions`, and `productVariants`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-sessions.schema.ts` (9/13/2026, 2:08:36 PM)**: Introduced the `checkoutSessions` table. It referenced a `cartId` (unique), and similar to `carts`, supported `userId` or `guestCheckoutSessionId`. Key fields included `email`, `phone`, `currency`, and various financial totals (`subtotal`, `discountTotal`, `shippingTotal`, `taxTotal`, `grandTotal`), all defaulting to 0. It used a `checkout_status` enum, `expiresAt`, and timestamps. Multiple indices were added, along with a `check` constraint for "exactly one owner" and several `check` constraints to ensure financial totals are non-negative.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-addresses.schema.ts`**:
    *   **Initially (9/13/2026, 2:10:18 PM)**: Defined `checkoutAddresses` for storing shipping/billing details for a `checkoutSession`. Fields included `firstName`, `lastName`, `phone`, `addressLine1/2`, `city`, `county`, `postalCode`, `country` (default "KE"), and `deliveryInstructions`. An index was added on `checkoutSessionId`.
    *   **Later (9/13/2026, 2:13:52 PM)**: A `uniqueIndex` `checkout_addresses_checkout_unique` was added, ensuring each `checkoutSession` has only one associated address.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-relations.ts` (9/13/2026, 2:11:53 PM)**: This new file defined the Drizzle ORM relations for `checkoutSessions` and `checkoutAddresses`, linking them to `carts`, `users`, and `guestCheckoutSessions`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/index.ts`**:
    *   **Initially (9/13/2026, 1:49:33 PM)**: Updated to export `inventory-transactions.schema`, `carts.schema`, `cart-items.schema`, and `cart-relations`, reflecting the new additions.
    *   **Later (9/13/2026, 2:14:31 PM)**: Further updated to export `checkout-sessions.schema`, `checkout-addresses.schema`, and `checkout-relations`, maintaining its role as the central schema aggregation point.

### Timestamps of Significant Changes:

*   **9/13/2026, 11:11 AM - 11:25 AM**: Initial definitions and immediate refinements for product variants, inventory, pricing, and inventory transactions. This period focused heavily on ensuring data integrity with `check` constraints and `sql` imports.
*   **9/13/2026, 11:28 AM**: Introduction of `catalog-relations.ts`, centralizing inter-schema relationships for catalog and inventory.
*   **9/13/2026, 1:30 PM - 1:49 PM**: Rapid development of cart functionality, including `carts.schema.ts` (with unique indices for active carts), `cart-items.schema.ts`, and `cart-relations.ts`.
*   **9/13/2026, 1:49 PM & 2:14 PM**: Consistent updates to `index.ts` to export newly created schema and relation files.
*   **9/13/2026, 2:08 PM - 2:14 PM**: Development of checkout functionality, introducing `checkout-sessions.schema.ts`, `checkout-addresses.schema.ts` (with unique index), and `checkout-relations.ts`.

### Patterns and Recurring Elements:

*   **Drizzle ORM for Schema Definition**: All changes consistently use Drizzle ORM (`pgTable`, `uuid`, `varchar`, `integer`, `boolean`, `pgEnum`, `timestamp`) for defining database schemas, indicating a standardized approach to database modeling.
*   **UUID Primary Keys**: All tables utilize `uuid("id").defaultRandom().primaryKey()` for their primary identifiers, ensuring globally unique keys.
*   **Timestamps**: `createdAt` and `updatedAt` fields with `defaultNow()` are almost universally included in new tables, often alongside `expiresAt` or `deletedAt` for specific lifecycle management.
*   **Robust Data Integrity**: A strong emphasis is placed on data integrity through extensive use of:
    *   `references()` with `onDelete` actions (`cascade`, `restrict`, `set null`) for foreign key constraints.
    *   `index()` for query optimization.
    *   `uniqueIndex()` to enforce uniqueness on specific combinations of columns, often with `where` conditions (e.g., active carts).
    *   `check` constraints with `sql` expressions to enforce business rules like non-negative quantities, positive values, non-zero changes, and mutual exclusivity of foreign keys (e.g., `carts_exactly_one_owner`).
*   **Enum Usage**: `pgEnum` is frequently employed for status fields (e.g., `cart_status`, `checkout_status`, `variant_status`) and type fields (`inventory_transaction_type`), promoting type safety and limiting valid values.
*   **Modular Schema Design**: The project adopts a clear modular structure, with individual files for table schemas (`.schema.ts`) and separate files for defining Drizzle ORM relations (`-relations.ts`), all consolidated through a central `index.ts` for export.
*   **Incremental Development**: The log demonstrates a logical progression, building foundational catalog and inventory features before moving to transactional components like carts and checkout, with each step involving initial schema definition followed by refinement with data integrity checks.

## 4:12:43 PM
The code changes primarily involve defining and refining Drizzle ORM schemas for a "nuraya-fragrance" project, focusing on catalog, inventory, cart, and checkout functionalities. All changes occurred on **September 13, 2026**.

**File-specific Updates:**

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts`**:
    *   Initial schema for `productVariants` was defined at **11:11:23 AM**, including fields like `productId`, `sku`, `sizeValue`, `concentration`, and `status`, along with `concentrationEnum` and `variantStatusEnum`.
    *   The `sql` import was added at **11:11:52 AM**.
    *   A `check` constraint `product_variants_size_value_positive` (ensuring `sizeValue > 0`) was added and refined between **11:15:26 AM** and **11:18:43 AM**, with a brief duplication at **11:17:42 AM** that was subsequently corrected.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts`**:
    *   The `inventory` schema was introduced at **11:19:29 AM**, linking to `productVariants` and including `quantity` and `reservedQuantity`.
    *   The `sql` import was added at **11:19:49 AM**.
    *   Three `check` constraints were added at **11:20:35 AM** to enforce non-negative `quantity` and `reservedQuantity`, and to ensure `reservedQuantity` does not exceed `quantity`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts`**:
    *   The `pricing` schema was created at **11:21:37 AM**, defining price `amount`, `currency`, and optional `compareAtAmount` for product variants.
    *   At **11:22:13 AM**, `check` constraints were added to ensure `amount` and `compareAtAmount` (if present) are non-negative.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts`**:
    *   The `inventoryTransactions` schema and `inventoryTransactionTypeEnum` were defined at **11:23:33 AM**, capturing changes to inventory with a `quantityChange` and `type`.
    *   The `sql` import was added at **11:23:46 AM**.
    *   A `check` constraint `inventory_transactions_quantity_change_non_zero` was added at **11:24:22 AM**, ensuring `quantityChange` is never zero.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts`**:
    *   Introduced at **11:28:35 AM**, this file centralizes Drizzle ORM relationship definitions for catalog entities like `products`, `productVariants`, `brands`, `categories`, `inventory`, and `inventoryTransactions`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/carts.schema.ts`**:
    *   The `carts` schema and `cartStatusEnum` were defined at **1:30:22 PM**, handling user-specific or guest-specific carts and their status. A `check` constraint `carts_exactly_one_owner` ensures a cart belongs to either a user or a guest, but not both.
    *   Between **1:42:42 PM** and **1:45:46 PM**, `uniqueIndex`es were added for `carts_active_user_unique` and `carts_active_guest_unique`, preventing a user or guest from having multiple active carts.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-items.schema.ts`**:
    *   Created at **1:47:17 PM**, this schema defines items within a cart, linking to `carts` and `productVariants`, with a `uniqueIndex("cart_items_cart_variant_unique")` to prevent duplicate variant entries in a single cart, and a `check` constraint `cart_items_quantity_positive` for positive quantities.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-relations.ts`**:
    *   Added at **1:49:04 PM**, this file defines relationships between `carts`, `cartItems`, `users`, `guestCheckoutSessions`, and `productVariants`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/index.ts`**:
    *   This central export file was updated twice (**1:49:33 PM** and **2:14:31 PM**) to include the newly created schema and relation files for catalog, inventory, carts, and checkout modules.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-sessions.schema.ts`**:
    *   Defined at **2:08:36 PM**, this schema introduces `checkoutSessions` and `checkoutStatusEnum`, linking to `carts`, `users`, and `guestCheckoutSessions`. It includes detailed financial fields (subtotal, grand total, etc.) with `check` constraints for non-negative values, and an `exactly_one_owner` constraint.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-addresses.schema.ts`**:
    *   Initial schema for `checkoutAddresses` was created at **2:10:18 PM**, storing shipping/billing address details for a `checkoutSession`.
    *   A `uniqueIndex("checkout_addresses_checkout_unique")` was added at **2:13:52 PM** to ensure each checkout session has only one address.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-relations.ts`**:
    *   Added at **2:11:53 PM**, this file defines relationships for `checkoutSessions` and `checkoutAddresses`, connecting them to `carts`, `users`, and `guestCheckoutSessions`.

**Patterns and Recurring Elements:**

*   **Drizzle ORM Adoption**: All schemas are defined using Drizzle ORM, leveraging its `pgTable`, `uuid`, `varchar`, `integer`, `boolean`, `pgEnum`, `index`, `timestamp`, and `check` features.
*   **UUID Primary Keys**: Every table consistently uses `uuid("id").defaultRandom().primaryKey()` for its primary identifier.
*   **Timestamping**: `createdAt` and `updatedAt` fields, defaulting to `defaultNow()`, are ubiquitous for tracking record lifecycle. `deletedAt` and `expiresAt` are also used where applicable.
*   **Foreign Key Constraints**: Relationships between tables are established with `references()` and explicit `onDelete` actions (e.g., `cascade`, `restrict`, `set null`).
*   **Data Integrity with `check` constraints**: Frequent use of `sql` expressions within `check` constraints to enforce business rules like non-negative quantities, positive values, and mutually exclusive ownership (e.g., `userId` XOR `guestCheckoutSessionId`).
*   **Indexing for Performance**: `index()` and `uniqueIndex()` are extensively used across tables to optimize query performance and enforce uniqueness on key fields.
*   **Enum Types**: Custom PostgreSQL enum types (e.g., `concentrationEnum`, `cartStatusEnum`) are a common pattern for defining allowed values for status or type fields.
*   **Modular Schema Design**: The project adopts a modular structure, separating schema definitions (`.schema.ts`) from their relationship definitions (`.relations.ts`). A central `index.ts` aggregates all these exports.
*   **Incremental Refinement**: The log shows an iterative development process, starting with basic schema definitions and progressively adding `sql` imports, `check` constraints, and `uniqueIndex`es to enhance data integrity and enforce specific application logic.

## 8:40:07 PM
The code changes, all occurring on 9/13/2026, primarily focus on expanding and refining the database schema for an e-commerce platform using Drizzle ORM. This includes detailed definitions for product variants, inventory, pricing, shopping carts, and checkout processes, along with establishing their relationships.

**File-specific updates:**

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts**:
    *   Initial schema for `product_variants` was created at 11:11:23 AM, defining attributes like `productId`, `sku`, `sizeValue`, `concentration` (using a `fragrance_concentration` enum), `isDefault`, and `status` (using a `variant_status` enum).
    *   An `sql` import was added at 11:11:52 AM, followed by the addition of a `check` constraint (`product_variants_size_value_positive`) to ensure `sizeValue` is greater than 0 (11:15:26 AM). A brief duplication of this check constraint was corrected between 11:17:42 AM and 11:18:43 AM.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts**:
    *   The `inventory` schema was established at 11:19:29 AM, including `variantId`, `quantity`, and `reservedQuantity` fields.
    *   An `sql` import was added at 11:19:49 AM, and at 11:20:35 AM, three `check` constraints were introduced to enforce non-negative quantities and ensure `reservedQuantity` does not exceed `quantity`.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts**:
    *   The `pricing` schema was defined at 11:21:37 AM, with `variantId`, `currency` (default "KES"), `amount`, and `compareAtAmount`.
    *   By 11:22:13 AM, `check` constraints were added to ensure `amount` and `compareAtAmount` are non-negative.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts**:
    *   The `inventory_transactions` schema was created at 11:23:33 AM, logging changes in inventory (`inventoryId`, `type` using `inventory_transaction_type` enum, `quantityChange`, `reason`, `performedBy`).
    *   After an `sql` import at 11:23:46 AM, a `check` constraint was added at 11:24:22 AM to ensure `quantityChange` is not zero.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts**:
    *   This file was introduced at 11:28:35 AM to define Drizzle ORM relations between various catalog entities such as products, brands, categories, product variants, images, notes, inventory, pricing, and inventory transactions.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/carts.schema.ts**:
    *   The `carts` schema was established at 1:30:22 PM, containing `userId`, `guestCheckoutSessionId`, `status` (from `cart_status` enum), and `expiresAt`. It includes a `check` constraint (`carts_exactly_one_owner`) to ensure a cart belongs to either a registered user or a guest session, but not both.
    *   Further updates added `uniqueIndex` imports (1:42:42 PM) and then two `uniqueIndex` constraints (1:44:47 PM and 1:45:46 PM) to ensure uniqueness for active carts per user and per guest session respectively.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-items.schema.ts**:
    *   The `cart_items` schema was created at 1:47:17 PM, linking `cartId` with `productVariantId` and specifying `quantity`. It features a `uniqueIndex` for preventing duplicate variant entries in a single cart and a `check` constraint (`cart_items_quantity_positive`) to ensure quantity is positive.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-relations.ts**:
    *   This file was created at 1:49:04 PM to define Drizzle ORM relations for `carts` (linking to users, guest checkout sessions, and cart items) and `cartItems` (linking to carts and product variants).

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/index.ts**:
    *   This central export file was updated twice (1:49:33 PM and 2:14:31 PM) to include all the newly created schema and relation files (`inventory-transactions.schema`, `carts.schema`, `cart-items.schema`, `cart-relations`, `checkout-sessions.schema`, `checkout-addresses.schema`, and `checkout-relations`).

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-sessions.schema.ts**:
    *   The `checkout_sessions` schema was introduced at 2:08:36 PM. It holds comprehensive details including `cartId`, `userId`, `guestCheckoutSessionId`, `email`, `phone`, `currency`, various total amounts (`subtotal`, `discountTotal`, `shippingTotal`, `taxTotal`, `grandTotal`), `status` (via `checkout_status` enum), and `expiresAt`. It also includes `check` constraints for non-negative totals and an "exactly one owner" rule similar to `carts`.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-addresses.schema.ts**:
    *   The `checkout_addresses` schema was created at 2:10:18 PM to store delivery address details (e.g., `firstName`, `lastName`, `phone`, `addressLine1`, `city`, `country`) linked to a `checkoutSessionId`.
    *   At 2:13:52 PM, a `uniqueIndex` (`checkout_addresses_checkout_unique`) was added to ensure each checkout session can only have one associated address.

*   **/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-relations.ts**:
    *   This file was created at 2:11:53 PM to define Drizzle ORM relations for `checkoutSessions` (linking to carts, users, guest sessions, and checkout addresses) and `checkoutAddresses` (linking to checkout sessions).

**Patterns and Recurring Elements:**

*   **Consistent Drizzle ORM Usage:** All schema definitions consistently use `drizzle-orm` with `pgTable` for PostgreSQL, including `uuid` for primary keys with `defaultRandom()`.
*   **Timestamping:** Most tables include `createdAt` and `updatedAt` columns with `defaultNow()`, often using a `...timestamps` helper, for automatic record tracking.
*   **Foreign Key Integrity:** Extensive use of `references` with `onDelete` actions (`"cascade"`, `"restrict"`, `"set null"`) is seen, demonstrating a focus on relational data integrity.
*   **`sql` for Custom Constraints:** The `sql` utility from Drizzle ORM is frequently imported and utilized to implement complex `check` and `where` conditions for indexes, enforcing detailed business logic directly in the database.
*   **Indexed for Performance & Uniqueness:** Multiple `index` and `uniqueIndex` definitions are present across various schemas, suggesting an emphasis on efficient data retrieval and maintaining data integrity.
*   **Enum Types for Categorization:** Custom `pgEnum` types are widely used (e.g., `fragrance_concentration`, `variant_status`, `cart_status`, `inventory_transaction_type`, `checkout_status`) to categorize and validate data entries.
*   **Modular Schema Design:** Schemas are separated into distinct files based on domain, and dedicated "relations" files (`catalog-relations.ts`, `cart-relations.ts`, `checkout-relations.ts`) manage inter-table relationships, with a central `index.ts` aggregating all exports.
*   **"Exactly One Owner" Logic:** Both `carts` and `checkout_sessions` implement a `check` constraint to ensure ownership by either a `userId` or a `guestCheckoutSessionId`, but not both, indicating a standardized approach for managing user vs. guest data.

## 9:48:40 PM
The changes log details the progressive development and refinement of database schemas and their relationships using Drizzle ORM for a "nuraya-fragrance" project.

### File-Specific Updates:

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/product-variants.schema.ts`**
    *   **9/13/2026, 11:11:23 AM:** Initial schema definition for `productVariants` table, including `id` (UUID), `productId` (foreign key to `products`), `sku` (unique), `barcode`, `sizeValue`, `sizeUnit` (default "ML"), `concentration` (using `fragrance_concentration` enum), `isDefault`, `status` (using `variant_status` enum), `createdAt`, `updatedAt`, and `deletedAt`. Two enums, `concentrationEnum` and `variantStatusEnum`, were also defined. Indexes were added for `productId` and `sku`.
    *   **9/13/2026, 11:11:52 AM:** Added an import for `sql` from `drizzle-orm`.
    *   **9/13/2026, 11:15:26 AM - 11:18:43 AM:** A `check` constraint was introduced to ensure `sizeValue` is positive (`sizeValue > 0`). There was a temporary duplication of this check constraint at 11:17:42 AM, which was resolved in the subsequent commit at 11:18:43 AM, correctly integrating it into the table definition's functional argument alongside indexes.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory.schema.ts`**
    *   **9/13/2026, 11:19:29 AM:** Initial schema for the `inventory` table, comprising `id` (UUID), `variantId` (unique foreign key to `productVariants`), `quantity` (default 0), `reservedQuantity` (default 0), `createdAt`, and `updatedAt`. An index on `variantId` was defined.
    *   **9/13/2026, 11:19:49 AM:** Added an import for `sql` from `drizzle-orm`.
    *   **9/13/2026, 11:20:35 AM:** Added three `check` constraints: `quantity` must be non-negative, `reservedQuantity` must be non-negative, and `reservedQuantity` must not exceed `quantity`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/pricing.schema.ts`**
    *   **9/13/2026, 11:21:37 AM:** Initial schema for the `pricing` table, including `id` (UUID), `variantId` (foreign key to `productVariants`), `currency` (default "KES"), `amount`, `compareAtAmount` (optional), `activeFrom` (optional), `activeUntil` (optional), and `createdAt`. An index on `variantId` was created.
    *   **9/13/2026, 11:22:13 AM:** Added two `check` constraints: `amount` must be non-negative, and `compareAtAmount` must either be `NULL` or non-negative.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/inventory-transactions.schema.ts`**
    *   **9/13/2026, 11:23:33 AM:** Initial schema for `inventoryTransactions` table. It defined `inventoryTransactionTypeEnum` (e.g., "RESTOCK", "SALE", "RETURN"). The table includes `id` (UUID), `inventoryId` (foreign key to `inventory`), `type` (using enum), `quantityChange`, `reason` (optional text), `performedBy` (foreign key to `users` with `onDelete: "set null"`), and `createdAt`. Multiple indexes were added for efficient querying.
    *   **9/13/2026, 11:23:46 AM:** Added an import for `sql` from `drizzle-orm`.
    *   **9/13/2026, 11:24:22 AM:** Added a `check` constraint to ensure `quantityChange` is not zero. No further changes were logged for this file at 11:24:54 AM.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/catalog-relations.ts`**
    *   **9/13/2026, 11:28:35 AM:** This file was created to define relations between various catalog-related schemas using Drizzle ORM's `relations` function. It established relationships for `products`, `productVariants`, `productImages`, `fragranceNotes`, `brands`, `categories`, `inventory`, `inventoryTransactions`, and `productNotes`, linking them to their respective related tables.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/carts.schema.ts`**
    *   **9/13/2026, 1:30:22 PM:** Initial schema for the `carts` table. It introduced `cartStatusEnum` (e.g., "ACTIVE", "CHECKOUT", "ABANDONED"). The table includes `id` (UUID), `userId` (foreign key to `users`), `guestCheckoutSessionId` (foreign key to `guestCheckoutSessions`), `status` (using enum), `expiresAt`, `createdAt`, and `updatedAt`. Several indexes were added, along with a `check` constraint (`carts_exactly_one_owner`) to ensure a cart is owned by either a user or a guest session exclusively.
    *   **9/13/2026, 1:42:42 PM:** The `uniqueIndex` import was added, but no functional change was made in this commit.
    *   **9/13/2026, 1:44:47 PM:** A `uniqueIndex` (`carts_active_user_unique`) was added on `userId` filtered by `status = 'ACTIVE'`, enforcing that a user can only have one active cart.
    *   **9/13/2026, 1:45:46 PM:** Another `uniqueIndex` (`carts_active_guest_unique`) was added on `guestCheckoutSessionId` filtered by `status = 'ACTIVE'`, enforcing that a guest session can also only have one active cart.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-items.schema.ts`**
    *   **9/13/2026, 1:47:17 PM:** Initial schema for `cartItems` table, defining `id` (UUID), `cartId` (foreign key to `carts`), `productVariantId` (foreign key to `productVariants`), `quantity`, `createdAt`, and `updatedAt`. Indexes were added on `cartId` and `productVariantId`. A `uniqueIndex` (`cart_items_cart_variant_unique`) was added to ensure each product variant appears only once per cart, and a `check` constraint ensures `quantity` is positive.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/cart-relations.ts`**
    *   **9/13/2026, 1:49:04 PM:** This file was created to define relations for cart entities. It established `carts` relations to `user`, `guestCheckoutSession`, and `items`, and `cartItems` relations to `cart` and `productVariant`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-sessions.schema.ts`**
    *   **9/13/2026, 2:08:36 PM:** Initial schema for `checkoutSessions` table. It defines `checkoutStatusEnum` (e.g., "DRAFT", "PROCESSING", "COMPLETED"). The table includes `id` (UUID), `cartId` (unique foreign key to `carts`), `userId` (foreign key to `users`), `guestCheckoutSessionId` (foreign key to `guestCheckoutSessions`), `email`, `phone`, `currency` (default "KES"), `subtotal`, `discountTotal`, `shippingTotal`, `taxTotal`, `grandTotal` (all integer defaults 0), `status` (using enum), `expiresAt`, `createdAt`, and `updatedAt`. It includes indexes, a `uniqueIndex` on `cartId`, an "exactly one owner" `check` constraint, and several `check` constraints to ensure monetary fields are non-negative.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-addresses.schema.ts`**
    *   **9/13/2026, 2:10:18 PM:** Initial schema for `checkoutAddresses` table, including `id` (UUID), `checkoutSessionId` (foreign key to `checkoutSessions`), `firstName`, `lastName`, `phone`, `addressLine1`, `addressLine2` (optional), `city`, `county` (optional), `postalCode` (optional), `country` (default "KE"), `deliveryInstructions` (optional text), `createdAt`, and `updatedAt`. An index on `checkoutSessionId` was defined.
    *   **9/13/2026, 2:13:52 PM:** A `uniqueIndex` (`checkout_addresses_checkout_unique`) was added on `checkoutSessionId` to ensure only one shipping address is associated with a given checkout session.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/checkout-relations.ts`**
    *   **9/13/2026, 2:11:53 PM:** This file was created to define relations for checkout entities. It established `checkoutSessions` relations to `cart`, `user`, `guestCheckoutSession`, and `address`, and `checkoutAddresses` relation to `checkoutSession`.

*   **`/Users/u/Projects/nuraya-fragrance/src/db/schema/index.ts`**
    *   **9/13/2026, 1:49:33 PM:** Updated to export new schemas and relations: `inventory-transactions.schema`, `carts.schema`, `cart-items.schema`, and `cart-relations`.
    *   **9/13/2026, 2:14:31 PM:** Further updated to export `checkout-sessions.schema`, `checkout-addresses.schema`, and `checkout-relations`.

### Patterns and Recurring Elements:

*   **Drizzle ORM Adoption:** All schema definitions consistently use Drizzle ORM syntax for PostgreSQL, defining tables, columns, enums, indexes, and constraints.
*   **UUID Primary Keys:** All new tables (`productVariants`, `inventory`, `pricing`, `inventoryTransactions`, `carts`, `cartItems`, `checkoutSessions`, `checkoutAddresses`) utilize `uuid("id").defaultRandom().primaryKey()` for their primary keys, ensuring unique identifiers.
*   **Timestamp Fields:** `createdAt` and `updatedAt` fields with `defaultNow()` are ubiquitous, indicating a standard practice for tracking record creation and modification times. `deletedAt` is also used for soft deletes in `productVariants`.
*   **Enums for Status and Type:** `pgEnum` is widely used to define discrete states and types (e.g., `concentrationEnum`, `variantStatusEnum`, `inventoryTransactionTypeEnum`, `cartStatusEnum`, `checkoutStatusEnum`), enhancing data integrity and readability.
*   **Foreign Key Constraints:** Extensive use of `.references()` to define foreign key relationships between tables, often with `onDelete` actions like `"cascade"`, `"restrict"`, or `"set null"`, to manage data consistency upon deletion of referenced records.
*   **Data Integrity `check` Constraints:** A significant pattern is the frequent addition of `check` constraints using `sql` to enforce business rules and data validation at the database level. Examples include ensuring quantities are non-negative, amounts are positive, and conditional logic (e.g., `carts_exactly_one_owner`, `inventory_reserved_not_exceed_quantity`). This highlights a strong emphasis on robust data validation.
*   **Indexing for Performance:** Indexes are consistently added on foreign keys (`productId`, `variantId`, `cartId`, `checkoutSessionId`, `inventoryId`, `performedBy`) and frequently queried fields (e.g., `sku`, `status`, `expiresAt`, `type`, `createdAt`) to optimize database query performance.
*   **Unique Indexes for Business Logic:** `uniqueIndex` is used to enforce uniqueness for specific filtered conditions or combinations of columns, such as ensuring a user or guest has only one active cart, or that a checkout session has only one associated address.
*   **Modular Relations Files:** The creation of separate `*-relations.ts` files (`catalog-relations.ts`, `cart-relations.ts`, `checkout-relations.ts`) demonstrates a modular approach to organizing Drizzle ORM relations, improving code structure and maintainability.
*   **Central `index.ts` for Export:** The `src/db/schema/index.ts` file is regularly updated to re-export all new schema and relation files, serving as a central entry point for accessing the database schema definitions.